/**
 * Scene 28 — content owned by VerifyStage (comp 6776–7615): the Flow capture
 * (centred) + Flow Detail highlight. This scene renders only background.
 */
import { SafeArea } from "../components";

export const Scene28 = () => <SafeArea>{null}</SafeArea>;
