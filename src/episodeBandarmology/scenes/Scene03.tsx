/**
 * Scene 03 (571, dur 175) — intentionally empty. This window is covered by the
 * BandarTitle card (comp 570–750, "Bandarmology / Following the big players"),
 * which clears all other visuals. The previous order-book content was removed per
 * direction; nothing is restored after the title.
 */
import { SafeArea } from "../components";

export const Scene03 = () => <SafeArea>{null}</SafeArea>;
