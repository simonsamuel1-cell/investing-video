/**
 * Scene 01 — Order book: retail price ladder (0, dur 207). All visible content
 * (scaled phone + timed captions + row highlights) is owned by the S0102Stage
 * continuity layer so it can span the S01/S02 boundary on absolute frames; this
 * scene renders only the background.
 */
import { SafeArea } from "../components";

export const Scene01 = () => <SafeArea>{null}</SafeArea>;
