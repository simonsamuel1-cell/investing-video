import { ScenePlaceholder } from "../components/ScenePlaceholder";

export const Scene09 = () => <ScenePlaceholder n={9} cardTitle="STOCK A" label="Probability, not prediction — what a chart shows / doesn't" />;
