import { ScenePlaceholder } from "../components/ScenePlaceholder";

export const Scene03 = () => <ScenePlaceholder n={3} cardTitle="STOCK A — Daily" label="A record of every decision — the trade tape" />;
