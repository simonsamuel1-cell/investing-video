/**
 * Scene 25 — Card 3 checkpoint (comp 6040–6154, dur 114). The "Confirmation" chip
 * was removed; card 3 lands via the RecapFrame StepRail. This scene now carries no
 * overlay. Frame = scene-local.
 */
import { AbsoluteFill } from "remotion";

export const Scene25 = () => <AbsoluteFill style={{ pointerEvents: "none" }} />;
